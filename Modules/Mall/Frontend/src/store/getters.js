import getData from "@/utils/getData"


// 对Vuex的变量进行过滤
const getters = {
    // getCompanyDetail(state, id) {
    //     if(state.Company_Detail == null) {
    //        return  getData.onGetCompanyDetail(id)
    //                 // .then(res => {
    //                 //     this.formData = res; console.log(res)
    //                 // })
    //     }
    // }
}

export default getters